# Virtual remote

## Quick start
Copy `example.env` to `.env` and change the values.

### Docker
```docker build . -t mdarvanaghi/virtual-remote```

```docker run -d -p 8000:8000 mdarvanaghi/virtual-remote```

### Windows
Run `run_windows.bat`